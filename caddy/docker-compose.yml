services:
  caddy:
    image: caddy:latest
    container_name: caddy
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8443:8443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - /data/caddy:/data
    networks:
      - homelab
    logging:
      driver: json-file
      options:
        max-size: 1M
        max-file: "2"

networks:
  homelab:
    name: homelab
    driver: bridge
